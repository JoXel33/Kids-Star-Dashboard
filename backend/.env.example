# Server
PORT=3000

# HMAC secret used to hash access codes and recovery answers.
# In production, replace with a long random string. NEVER commit a real value.
SERVER_SECRET=please-replace-with-a-long-random-string

# SQLite database file (path is relative to the backend/ directory).
DB_PATH=./data/dashboard.sqlite
